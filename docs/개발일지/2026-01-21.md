# 개발일지 - 2026-01-21

## 작업 요약
- FSD 구조에 맞춰 헤더/푸터 위젯 슬라이스 생성.
- 헤더: 좌측 Logo 텍스트, 반투명 배경, sticky 상단 고정.
- 푸터: 5개 탭(추가기능/레포트/홈/채팅/마이페이지) 아이콘+라벨 배치, 하단 고정.
- 홈 페이지에 헤더/푸터 연동.
- 전역 토큰 추가: `--app-header-height`, `--app-footer-height`.
- Tailwind 커스텀 높이 등록: `h-app-header`, `h-app-footer`.
- `/` 경로에서 홈 탭 활성 색상 적용.
- 홈 콘텐츠의 불필요한 스크롤 제거(`min-h-screen` 제거).

## 구현 상세
- 헤더 위젯
  - `src/widgets/header/ui/Header.tsx`, `src/widgets/header/index.ts`
  - `h-app-header` 적용
- 푸터 위젯
  - `src/widgets/footer/ui/Footer.tsx`, `src/widgets/footer/index.ts`
  - `h-app-footer` 적용, `fixed bottom-0`로 하단 고정
- 전역 레이아웃
  - `src/app/layout.tsx`에서 전역 헤더 제거 후 홈 페이지에서만 렌더
- 홈 페이지
  - `src/app/page.tsx`에 헤더/푸터 연결

## 다음 작업 메모
- 다른 페이지에서도 헤더/푸터 노출 여부 결정
- 푸터 탭 링크 연결 및 활성 상태 확장

## 회원가입 온보딩 UI 수정
- 라우팅 흐름 정리
  - `/onboarding` 진입 시 역할 선택 UI를 렌더하도록 변경.
  - 선택 버튼 클릭 시 `router.push('/onboarding/profile?role=EXPERT|SEEKER')`로 이동하도록 처리.
  - `/onboarding/profile`에서 `searchParams`를 `await`로 해제한 뒤 `role`을 소문자로 정규화(`expert`/`seeker`)해 폼으로 전달.
  - 폼(`OnboardingProfileForm`)에서도 `useSearchParams()`로 `role`을 읽어 서버/클라 경로 모두에서 일관되게 역할 표시되도록 보완.
- 역할 표시 UI 적용 방식
  - 상단 “선택한 유형” 영역을 칩 형태로 변경하고, `role` 값에 따라 `구직자/현직자` 텍스트를 렌더.
  - 선택된 역할이 바뀌면 해당 칩 텍스트도 즉시 변경되도록 연결.
- 구조/리팩터링 메모
  - `OnboardingProfileForm`에 UI 수정이 누적되어 파일이 길어졌고, 관심사가 섞여 있음.
  - 카드 섹션(직무/경력/기술스택), 입력 섹션(닉네임/자기소개), 바텀시트 영역으로 분리 필요.
  - 다음 작업에서 `ui/` 하위 컴포넌트로 쪼개고 props 경로 정리 권장.
